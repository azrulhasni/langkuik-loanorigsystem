import {
  cleave_esm_default
} from "./chunk-AIN4A3U3.js";
import "./chunk-KKNQZGBY.js";

// node_modules/@vaadin/flow-frontend/jh-textfield-formatter/jh-textfield-formatter.js
import { PolymerElement } from "@polymer/polymer/polymer-element.js";
var JhTextfieldFormatter = class extends PolymerElement {
  static get is() {
    return "jh-textfield-formatter";
  }
  static get properties() {
    return {
      conf: {
        type: Object,
        observer: "_confChanged"
      },
      cleave: {
        type: Object
      }
    };
  }
  connectedCallback() {
    super.connectedCallback();
    if (this.conf && !this.cleave) {
      this.cleave = new cleave_esm_default(this.parentElement.shadowRoot.querySelector("input"), this.conf);
    }
  }
  disconnectedCallback() {
    super.disconnectedCallback();
    if (this.cleave) {
      this.cleave.destroy();
      this.cleave = void 0;
    }
  }
  _confChanged(newConf, oldConf) {
    if (!newConf) {
      return;
    }
    this.conf = newConf;
    if (newConf.creditCard) {
      newConf.onCreditCardTypeChanged = (type) => {
        this.$server.onCreditCardChanged(type);
      };
    }
    if (this.cleave) {
      this.cleave.destroy();
      this.cleave = void 0;
    }
    this.cleave = new cleave_esm_default(this.parentElement.shadowRoot.querySelector("input"), newConf);
  }
};
window.customElements.define(JhTextfieldFormatter.is, JhTextfieldFormatter);
//# sourceMappingURL=@vaadin_flow-frontend_jh-textfield-formatter_jh-textfield-formatter__js.js.map
